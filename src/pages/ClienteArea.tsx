type UserProfile = { id: string, email: string, nome: string, cognome: string, nome_utente: string, numero_telefono: string | null, ruolo?: string };
// Versione unificata dell'area cliente
import ClienteArea from './ClienteAreaNew';
export default ClienteArea;
